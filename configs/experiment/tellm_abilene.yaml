# @package _global_
defaults:
    - /pipline: te
    - /model: tellm
    - override /dataset:
        - abilene


training:
  optimizer:
    # _target_: torch.optim.Adam     # Optimizer type
    lr: 1e-3
  trainer:
    max_epochs: 3
  scheduler:
    num_training_steps: 24000
